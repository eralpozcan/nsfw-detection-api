# API Anahtarı (Güvenlik için değiştirin)
API_KEY=your-production-api-key-here

# Port ayarı (opsiyonel)
PORT=3000

# Node environment
NODE_ENV=production

# Rate Limit Ayarları
RATE_LIMIT_WINDOW_MINUTES=15  # 15 minutes (15 * 60 * 1000)
RATE_LIMIT_MAX_REQUESTS=100  # 100 requests per window